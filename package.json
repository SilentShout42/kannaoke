{
  "name": "kannaoke",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "prepare:data": "node scripts/build-performances.mjs",
    "prepare:data:watch": "node --watch --watch-path=public/performances.json scripts/build-performances.mjs",
    "dev": "npm run prepare:data && (npm run prepare:data:watch & vite)",
    "build": "npm run prepare:data && tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@tabler/icons-react": "^3.42.0",
    "fuse.js": "^7.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.0.0",
    "@cloudflare/workers-types": "^4.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "wrangler": "^4.87.0"
  }
}
